<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.northeastern.lifeassistant">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CheckGoogleFitInstalled"></activity>
        <activity android:name=".SpontaneousActive1" />
        <activity android:name=".HistoryInfoActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".StepsHistory" />
        <activity android:name=".SpontaneousStepCounterActivity" />
        <activity android:name=".StepsCounterActivity" />
        <activity android:name=".StepsActivity" />
        <activity android:name=".SpontaneousScreen" />
        <activity android:name=".ScheduleScreen" />
        <activity android:name=".ActivityScreen" />
        <activity android:name=".CreateActivityScreen" />
        <activity android:name=".MainActivity" />
        <activity android:name=".CreateEventScreen" />
        <activity android:name=".SpontaneousActive" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="utils.SchedulerService"
            android:exported="false" />

        <receiver
            android:name="utils.Alarm"
            android:process=":remote" />
    </application>

</manifest>